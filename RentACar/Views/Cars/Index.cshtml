@model List<Cars>

@{
    ViewData["Title"] = "Cars";
}

@if (TempData["basarili"] != null)
{
    <div class="alert alert-success" role="alert">
        @TempData["basarili"]
    </div>
}

<div class="container">
    <div class="row pt-3 rb-4">
        <div>
            <h1>@ViewData["Title"]</h1>
        </div>
    </div>
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>
                    Id
                </th>
                <th>
                    Brand Name
                </th>
                <th>
                    Car Name
                </th>
                <th>
                    Km
                </th>
                <th>
                    Fuel
                </th>
                 <th>
                    Price
                </th>
                <th>
                    Img
                </th>
            </tr>
    
        </thead>
        <tbody>  
          @*   @foreach (var cars in Model)
            {
                <tr>
                    <td>@cars.Id</td>
                    <td>@cars.CarBrandName</td>
                    <td>@cars.CarName</td>
                    <td>@cars.CarKm</td>
                    <td>@cars.CarFuel</td>
                    <td>@cars.CarPrice</td>
                    <td>@cars.ImgUrl</td>
                    <td><a asp-controller="Cars" asp-action="AddUpdate" asp-route-id="@car.Id" type="button" class="btn btn-success" style="width:150px;">Update</a></td> <!--asp-route-id=idyi bulur-->
                    <td><a asp-controller="Cars" asp-action="Delete" asp-route-id="@car.Id" type="button" class="btn btn-danger" style="width:150px;">Delete</a></td>

                </tr>
            } *@
        </tbody>
    </table>
    <div class="row pt-3 pb-2">
        <div>
            <a asp-controller="Cars" asp-action="AddUpdate" class="btn btn-lg btn-primary" type="button">
                Add new car 
            </a>
        </div>
    </div>
</div>
